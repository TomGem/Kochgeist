import { defineCollection } from 'astro:content';

// Empty content config to suppress "[WARN] Content config not loaded" message.
// This project uses SQLite (Drizzle ORM) instead of Astro Content Collections.
export const collections = {};
